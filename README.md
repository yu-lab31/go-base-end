# go-base-end
Gather some useful packages to compose a semi-finished backend.

# Set up

- Linux

```shell
PROJ_NAME="your-new-project's-name"
git clone git@github.com:yu-lab31/go-base-end.git $PROJ_NAME && cd $PROJ_NAME && make init name=$PROJ_NAME
```
